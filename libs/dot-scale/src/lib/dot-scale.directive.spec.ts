import { DotScaleDirective } from './dot-scale.directive';

describe('DotScaleDirective', () => {
  it('should create an instance', () => {
    const directive = new DotScaleDirective();
    expect(directive).toBeTruthy();
  });
});
